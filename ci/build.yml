---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: node
    tag: 24.16.0

inputs:
  - name: dp-table-builder-ui

outputs:
  - name: build
  - name: package
  - name: dp-table-builder-ui

run:
  path: dp-table-builder-ui/ci/scripts/build.sh
